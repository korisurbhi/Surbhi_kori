#include<stdio.h>
#include<conio.h>
int sum(int a, int b)
{
    return(a+b);
}
void main()
{
    int a,b,c;
    printf("Enter two numbers :");
    scanf("%d %d",&a,&b);
    c=sum(a,b);
    printf("Additiom of %d and %d is %d",a,b,c);
}